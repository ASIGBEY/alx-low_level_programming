#include "main.h"
#include <string.h>

/**
 * _strlen - funtion to get the length of a string
 * @s: sring pointer to passe to thsis funtion
 * Return: returns length of the string
 */
int _strlen(char *s)
{
	return (strlen(s));
}


